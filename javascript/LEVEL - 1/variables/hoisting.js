/* Hoisting is a javascript mechanism where variables and functions declarations are moved 
to the top of their scope before code execution */
console.log(firstName);
const firstName = 'Hello';
console.log(firstName);